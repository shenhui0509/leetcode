#include <bits/stdc++.h>
using namespace std;

string shortestPalindrome(string &s){
    string rev_s = string(s.rbegin(), s.rend());
    string l = s + "#" + rev_s;
}

int main(void)
{
    return 0;
}